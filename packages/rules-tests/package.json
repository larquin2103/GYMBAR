{
  "name": "@gymbar/rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:emulator": "firebase emulators:exec --only firestore \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "vitest": "^2.1.8"
  }
}
